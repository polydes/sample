plugins {
    id("com.stencyl.gradle.extension") version "1.0.0"
}

stencyl {
    fromJsonFile(file("stencyl-extension.json"))
}

dependencies {
    //using jars that are included with Stencyl:
    //compileOnly 'ch.qos.reload4j:reload4j'
    //compileOnly 'commons-io:commons-io'
}
