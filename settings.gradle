pluginManagement {
    repositories {
        if (settings.hasProperty("local_build")) {
            mavenLocal()
        }
        maven { url = "https://www.stencyl.com/dl/maven2/releases" }
        gradlePluginPortal()
    }
}

rootProject.name = 'sample'